WINNER GYM — Subscriptions layout polish

Fixes only the subscriptions screen presentation:
- Prevents the page from sliding underneath the left sidebar.
- Moves the title/breadcrumb to the left like the approved reference.
- Increases fonts, date labels, table text and icons.
- Balances KPI cards, filters, table and right analytics column.
- Keeps the table scroll inside its own card instead of clipping the page.
- No migrations, no database changes, no .env changes.
