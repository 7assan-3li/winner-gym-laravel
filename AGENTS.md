# WINNER GYM — Architectural & Coding Guidelines for AI Agents

هذا الملف يحدد القواعد الإلزامية التي يجب على أي مساعد ذكاء اصطناعي أو مطور الالتزام بها عند قراءة أو تعديل أو إضافة أي كود في مشروع **WINNER GYM**.

---

## 1. المبادئ المعمارية الصارمة (Strict Architectural Rules)

1. **فصل المسؤوليات (Layered Architecture):**
   - **`app/Livewire/`**: مخصص فقط لحالة الواجهة (UI state)، المدخلات، التحقق المبدئي، رفع الملفات، والتحكم بالنوافذ المنبثقة (Modals).
   - **`app/Services/`**: هو المكان الحصري لكتابة قواعد العمل (Business Logic)، العمليات المحاسبية، تحديثات المخزون، وسجلات التدقيق.
   - **ممنوع منعاً باتاً**: كتابة أي استعلامات تعديل مالي أو مخزني مباشر من داخل مكونات Livewire أو قوالب Blade أو ملفات JavaScript.

2. **الأمان المالي والمعاملات (Financial Transactions & Locks):**
   - أي عملية مالية (إنشاء اشتراك، تحصيل دفعة، عكس قسط، استرداد، مبيعات، مشتريات، مصروفات) **يجب** أن تنفذ داخل `DB::transaction()`.
   - يجب استخدام القفل التشاؤمي `lockForUpdate()` على السجلات المالية والمخزنية المتأثرة لمنع التعارضات (Race Conditions).

3. **سجل التدقيق الإلزامي (Mandatory Audit Trail):**
   - كل عملية إنشاء أو تعديل أو إلغاء أو عكس لحركة تشغيلية أو مالية يجب أن تسجل فوراً في جدول `audit_logs` باستخدام `AuditService`.

---

## 2. قواعد المحاسبة والعملات (Currency & Accounting Rules)

1. **العزل الكامل للعملات (Strict Currency Separation):**
   - النظام يتعامل مع عملتين مستقلتين: **الريال اليمني (YER)** و **الريال السعودي (SAR)**.
   - **يُمنع منعاً باتاً** دمج مبالغ من عملتين مختلفتين، أو إجراء تحويل آلي بسعر صرف غير مدعوم، أو جمع إيرادات العملتين في رقم واحد. كل عملة لها تقاريرها وأرصدتها المستقلة.

2. **سياسة السداد والتحويلات (`PaymentPolicy`):**
   - التحويلات البنكية أو المصرفية (مثل العمقي، البسيري، إلخ) تخضع لسياسة `PaymentPolicy`.
   - يجب التحقق من ملء `transfer_service`، و `transfer_reference`، و `proof_path` حسب الإعدادات المفعلة في النظام.

3. **الدقة العشرية والتقريب:**
   - الحقول المالية مخزنة بنوع `decimal(18, 2)`.
   - التقريب دائماً برقمين عشريين `round($val, 2)`، والمقارنات المالية للقيم العشرية تتم بهامش أمان `abs($a - $b) > 0.009`.

4. **المخزون ومتوسط التكلفة المرجح (Inventory Valuation):**
   - عند اعتماد أي فاتورة شراء جديدة، يتم تحديث تكلفة المنتج (`purchase_cost`) تلقائياً وفق معادلة **المتوسط المرجح المتحرك (Moving Weighted Average)** داخل `InventoryService`.
   - أي عملية بيع أو إلغاء بيع تسجل حركة مخزنية في `inventory_movements` مع كميات ما قبل وما بعد الحركة.

---

## 3. قواعد الاشتراكات والحضور (Subscriptions & Attendance)

1. **الاشتراكات:**
   - لا يمكن لعضو امتلاك أكثر من اشتراك سارٍ في نفس الوقت (`active`, `expiring_soon`, `financial_overdue`).
   - في حال تجديد الاشتراك مبكراً، يبدأ الاشتراك الجديد في اليوم التالي لانتهاء الاشتراك السابق تلقائياً.
   - في خطط التقسيط: لا تقل الدفعة الأولى عن **50%** من قيمة الاشتراك النهائية.
   - الاسترداد المالي (Refund) لا يتجاوز **50%** من قيمة الاشتراك الإجمالية أو إجمالي المبلغ المدفوع (أيهما أقل).

2. **تسجيل الحضور:**
   - يجب التحقق من: نشاط العضو، تطابق فترة العضو (رجال/نساء) مع ساعات العمل المحددة، صلاحية الاشتراك، وعدم وجود أقساط متأخرة (`financial_overdue`).
   - يتم تسجيل جميع محاولات الدخول (المقبولة والمرفوضة مع سبب الرفض) في `attendance_attempts`.

---

## 4. الواجهات والمظهر والتوافق (UI & Frontend Standards)

1. **الاتجاه واللغة:** دعم كامل للغة العربية واتجاه RTL.
2. **المظهر:** دعم الثيمين الداكن والفاتح (Dark/Light Mode) مع الحفاظ على ملفات التنسيق في `public/winner-gym/*.css` ومراعاة توافق الألوان والـ Contrast.
3. **تحديث الـ Cache:** في حال تعديل أي ملف CSS في `public/winner-gym/`، تأكد من تحديث الـ version/cachebuster إذا لزم الأمر.

---

## 5. مهارات التطوير الإلزامية (Developer Skill Reference)

للاطلاع على التعليمات البرمجية التفصيلية ونماذج الكود وإجراءات التطوير لكل وحدة، قم بمراجعة Skill المخصصة:
- **`winner-gym-dev`** الموجودة في `.agents/skills/winner-gym-dev/SKILL.md`.
